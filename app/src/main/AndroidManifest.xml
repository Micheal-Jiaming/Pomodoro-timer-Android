<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!--
      An exact alarm is what lets a finished session announce itself while the
      app is in the background. USE_EXACT_ALARM is the declaration intended for
      alarm and timer apps and needs no runtime prompt; the older permission
      covers Android 12, where it is granted by default.
    -->
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission
        android:name="android.permission.SCHEDULE_EXACT_ALARM"
        android:maxSdkVersion="32" />

    <!--
      allowBackup is off deliberately, overriding the platform default of true. The
      only stored data is the chosen theme and the completed-session count, so nothing
      valuable is lost by not backing it up. What it buys is consistency with the app's
      own claim: the listing and the README say the data stays on the device, and
      Android Auto Backup would copy shared_prefs to Google Drive using the *system's*
      network access — which this app's lack of an INTERNET permission does not
      prevent. An F-Droid privacy reviewer checks exactly that pairing.
    -->
    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PomodoroTimer">

        <activity
            android:name=".MainActivity"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|uiMode|density|fontScale"
            android:exported="true"
            android:label="@string/app_name"
            android:launchMode="singleTop"
            android:theme="@style/Theme.PomodoroTimer">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver
            android:name=".FinishReceiver"
            android:exported="false" />

    </application>

</manifest>
